import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  label: {
    paddingBottom: 4,
  },
  labelText: {
    color: '#777',
  },
  input: {
    height: 40,
    borderColor: '#ccc',
    borderRadius: 5,
    borderWidth: 1,
    paddingHorizontal: 10,
  },
});
